#! user/bin/perl
print "Enter your name: ";
my $name = <STDIN>;   # Reads a line from user input
chomp($name);         # Removes the newline at the end
print "Hello, $name!\n";
{print "enter two numbers"
my $num1 = <STDIN>;
my $num2 =<STDIN>;
if ($num1 < $num2)
{print "$num1 is less then $num2";}
}
